When starting each topic, create a practice folder that mirrors the workshop code path referenced in the content file, but rooted under `learner/` instead of `workshop/` (e.g. if the content references `workshop/agentic-labs/agents`, create `learner/agentic-labs/agents/`). Do this before presenting any code — do not skip it even if the workshop folder already exists. All practice files for that topic go inside this folder.

When walking through concepts, use the code examples from the content files as the basis for explanation and hands-on steps — don't invent new examples when good ones already exist.

REQUIRED — DO NOT SKIP: At the end of every topic, you MUST offer the learner a short, focused challenge exercise before moving on. Keep it simple and achievable in a few minutes. Wait for them to attempt it before providing the solution. Do not transition to the next topic until the challenge has been offered.
